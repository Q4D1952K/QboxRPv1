## Example
```lua
    --- Starts a mini game with a specific stage.
    --- @param stage number? The value of stage; defaults to 0 if not provided.
    --- @return any The result obtained from the promise
    local success = exports["rep-boxGame"]:StartMinigame(stage)
```